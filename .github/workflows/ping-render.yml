name: Render Keep Alive (5min cron)

on:
  schedule:
    - cron: "*/5 * * * *"   # 5分ごと実行
  workflow_dispatch:        # 手動実行も可

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Call Render Cron
        run: |
          curl -s https://nft-trend-line-bot.onrender.com/cron
